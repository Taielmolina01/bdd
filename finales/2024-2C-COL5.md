1.

```
SELECT nombre, apellido 
FROM (
    SELECT  legajo, 
            nombre, 
            apellido
    FROM Resultados
    GROUP BY legajo
    HAVING COUNT(DISTINCT año) >= 1 AND COUNT(DISTINCT puesto) = 1
)
```

2.
3.
4.
5.
6.